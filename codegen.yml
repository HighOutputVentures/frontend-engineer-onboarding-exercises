schema:
  - ${API_URL:http://api-site-staging.zenithpanel.com/graphql}
generates:
  ./types/types.d.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-document-nodes
